import { StringCapitalizePipe } from './string-capitalize.pipe';

describe('StringCapitalizePipe', () => {
  it('create an instance', () => {
    const pipe = new StringCapitalizePipe();
    expect(pipe).toBeTruthy();
  });
});
